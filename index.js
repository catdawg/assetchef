"use strict";

const assetchef = module.exports = {};

assetchef.recipe = require("./recipe");

/**
 * Loads a recipe to be used by subsequent cook calls
 * @param {Object} recipe 
 */
assetchef.loadRecipe = function() {
    
};

/**
 * Processes the assets using the recipe set by loadRecipe method.
 * @param {Object} changes 
 */
assetchef.cook = function() {

};